<!DOCTYPE html>
<html>
  <head>
    <title>Liveheats</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" integrity="sha512-F...your-integrity-key-here..." crossorigin="anonymous" referrerpolicy="no-referrer" />
  </head>

  <body>
    <%= render "layouts/navbar" %>

    <div class="background-image"></div>

    <% if flash[:alert].present? %>
      <p class="flash-alert"><%= flash[:alert] %></p>
    <% end %>
    <% if flash[:notice].present? %>
      <p class="flash-notice"><%= flash[:notice] %></p>
    <% end %>

    <%= yield %>
  </body>
</html>
